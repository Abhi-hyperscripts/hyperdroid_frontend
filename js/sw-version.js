// ── Single source of truth for service worker version ──
// Increment this on every deploy that changes JS/CSS/HTML files.
// Consumed by: firebase-messaging-sw.js (importScripts) and config.js (script tag).
const SW_VERSION = 96;
